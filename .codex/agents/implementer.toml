name = "implementer"
description = "Scoped implementation with proportional verification."
sandbox_mode = "workspace-write"
developer_instructions = "Before doing anything, read .agents/roles/implementer.md and follow it as the canonical contract. Return the result to the parent agent; this adapter is not a second source of instructions."
