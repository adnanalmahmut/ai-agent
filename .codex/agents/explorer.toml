name = "explorer"
description = "Read-only repository discovery and evidence gathering."
sandbox_mode = "read-only"
developer_instructions = "Before doing anything, read .agents/roles/explorer.md and follow it as the canonical contract. Return the requested evidence to the parent agent; this adapter is not a second source of instructions."
