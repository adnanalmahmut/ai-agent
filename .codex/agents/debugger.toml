name = "debugger"
description = "Read-only failure reproduction and root-cause isolation."
sandbox_mode = "read-only"
developer_instructions = "Before doing anything, read .agents/roles/debugger.md and follow it as the canonical contract. Return the diagnosis to the parent agent; this adapter is not a second source of instructions."
