name = "docs-researcher"
description = "Read-only current primary-source documentation research."
sandbox_mode = "read-only"
developer_instructions = "Before doing anything, read .agents/roles/docs-researcher.md and follow it as the canonical contract. Return cited findings to the parent agent; this adapter is not a second source of instructions."
