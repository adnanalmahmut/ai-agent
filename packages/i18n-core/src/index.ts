export {
  SUPPORTED_LOCALES,
  DEFAULT_LOCALE,
  LOCALE_META,
  isAppLocale,
  parseAppLocale,
  resolveAppLocale,
  getDirection,
} from './locales';

export type { AppLocale, AppDirection, LocaleMeta } from './locales';
