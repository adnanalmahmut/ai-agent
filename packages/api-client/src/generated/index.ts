/**
 * The generated OpenAPI boundary.
 *
 * `application-api.generated.ts` is written by `pnpm api:types` from the
 * Backend's own document and committed; nothing here is hand-edited. It is
 * re-exported through this file so that consumers import a stable subpath
 * rather than a generated filename.
 */
export type * from './application-api.generated';
