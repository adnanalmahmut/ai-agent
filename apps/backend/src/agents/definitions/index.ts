import type { AgentDefinition } from '../agent.types';

/** Production definitions stay empty until the first real agent feature. */
export const PRODUCTION_AGENT_DEFINITIONS: readonly AgentDefinition[] = [];
